apiVersion: 1

providers:
  - name: 'LLM Gateway Dashboards'
    orgId: 1
    folder: 'LLM Gateway'
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /var/lib/grafana/dashboards
